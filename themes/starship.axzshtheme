# AX-ZSH: Alex' Modular ZSH Configuration
# starship.axzshtheme: Enable external "Starship" Theme
# see <https://starship.rs/>

if (( $+commands[starship] )); then
	eval "$(starship init zsh)"
else
	echo "Oops, starship(1) command not found, can't enable theme!" >&2
fi
